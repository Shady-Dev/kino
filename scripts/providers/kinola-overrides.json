{
  "_comment": [
    "Evidence-backed classification overrides for kinola.py, applied BEFORE the default",
    "classifier so an entry decides rather than argues with it. Two directions:",
    "`include` publishes a film whose page fills no metadata field, `exclude` withholds an",
    "event the classifier would otherwise publish. The second is why the mechanism cannot",
    "be one-sided.",
    "",
    "Scope is a provider id plus the site's own /film/{slug}/ identifier. Never a title",
    "keyword: classifying on a word is what the adopted policy forbids, and a keyword list",
    "here would reintroduce it through the back door.",
    "",
    "Every entry carries action, reason, evidence and verified. `evidence` names where the",
    "claim was read and `verified` the day it was read, so a stale entry is visible rather",
    "than merely old.",
    "",
    "An `exclude` is how the policy's third state is asserted. The classifier only ever",
    "says `film` or `unresolved`: nothing on these pages marks a live act structurally, so",
    "`non-film` is a judgement a person records here and the run reports it as a confirmed",
    "non-film rather than pretending the classifier found it. An exclusion also takes",
    "precedence over generic metadata, which is the point: a billed gig whose page fills",
    "Ohjaus or Lajityyppi publishes without one.",
    "",
    "Every entry is revalidated on every run, by kinola.override_state, against the page as",
    "it stands: `active` when it changes the classifier's verdict, `redundant` when the",
    "classifier now reaches the same outcome alone, and `evidence-unavailable` when the",
    "event is not in the listing or its page was not read. The third is not the second. A",
    "film that has gone off programme proves nothing about whether its entry is still",
    "needed, so nothing is dropped on that basis. The verdict per entry is in the run log.",
    "",
    "The decision and the corrections behind it: docs/archive/2026-09-providers.md,",
    "\"Kinola: the publication policy, adopted\"."
  ],
  "overrides": [
    {
      "provider": "kinolaika",
      "slug": "a-fox-under-a-pink-moon",
      "action": "include",
      "reason": "A documentary screening whose page fills no metadata field, so the classifier leaves it unresolved.",
      "evidence": "kinolaika.fi/film/a-fox-under-a-pink-moon/ names it in prose: \"Esitettavana elokuvana on Mehrdad Oskouen dokumenttielokuva A Fox Under A Pink Moon (2025)\", with 76 min, K-16 and English subtitles beside it. Director, form and year are all stated; only the labelled fields are missing.",
      "verified": "2026-09-15"
    }
  ]
}
